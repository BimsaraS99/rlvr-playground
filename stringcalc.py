"""
stringcalc.py — the BUGGY starting code the agent must fix.

TODO (you, the environment engineer):
Write a version of `add` that is GENUINELY BROKEN — it should fail at least
some of the cases in task.md — but that is fixable.

Ideas for realistic bugs (pick one or two, don't make it impossible):
  - doesn't handle the empty string (crashes or returns wrong value)
  - forgets to strip spaces, so int(" 2 ") ... (actually works; pick a real bug)
  - ignores negative numbers
  - returns a string instead of an int
  - off-by-one / wrong operator

Keep the function name and signature as `add(numbers: str) -> int`.
"""


def add(numbers: str) -> int:
    # TODO: replace this with your intentionally-buggy implementation.
    raise NotImplementedError("write a buggy version here")
